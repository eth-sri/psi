
def main(){
    r := 1:ℝ; // TODO: this annotation should not be necessary
    repeat 3{
    	k := uniform(0,1);
        r *= k;
		p := k <= 1/2;
		observe(p);
    }
    p := r > 1/8;
    return p; // expected: δ(0)[p]
}
