def main(){
	return exp(exp(uniform(0,1))); // expected: [-1+log(log(r))≤0]·[-log(r)+1≤0]·[-r≤0]·[r≠0]·λ[r]·⅟log(r)·⅟r
	// TODO: compute normalization constaint
	// TODO: simplify Iverson brackets better
}
